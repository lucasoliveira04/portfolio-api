services:
  portfolio-api:
    build: .
    container_name: portfolio-api
    restart: unless-stopped
    mem_limit: 512m
    ports:
      - "8081:8081"
    env_file:
      - .env.prod
    networks:
      - database_homelab

networks:
  database_homelab:
    external: true